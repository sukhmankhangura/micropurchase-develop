<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- test content -->
    <meta charset="utf-8">
    <meta name="ROBOTS" content="NOODP" />
    <title><%= yield(:title).presence || "GDS - Micro-purchase" %></title>
    <meta property="og:title" content="<%= yield(:title).presence || "GDS - Micro-purchase" %>">

    <!-- Link to FontAwesome -->

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', debug: true %>
    <%= jquery_include_tag :google %>
    <%= javascript_include_tag "application", debug: true %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'hive-logo.png' %>

    <meta name="description" content="<%= yield(:description).presence || "The Micro-purchase Marketplace is the place to bid on open-source issues from the GDS team." %>">
    <meta property="og:description" content="<%= yield(:description).presence || "The Micro-purchase Marketplace is the place to bid on open-source issues from the GDS team." %>">

    <meta name="twitter:card" content="summary">

    <%= yield :data_tags %>

    <%= render partial: 'components/google_analytics' %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body class="site">
    <%= render partial: 'components/nav_header' %>

    <div class="site-content">
      <%= render "flashes" %>
      <%= yield %>
    </div>

    <%= render "components/footer" %>
    <%= render partial: 'components/dap' %>
  </body>
</html>
